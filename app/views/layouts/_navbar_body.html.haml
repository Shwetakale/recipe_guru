- if signed_in?
  %ul.nav.navbar-nav
    - unless false
      
      %li= link_to content_tag(:span, "", class: "glyphicon glyphicon-list-alt") + "  Recipes", "#" if signed_in?
  
      - if true
        %li.dropdown
          = link_to content_tag(:span, "", class: "glyphicon glyphicon-cog") + " " + content_tag(:b, "", class: "caret"), "#", class: "dropdown-toggle", data: { toggle: "dropdown" }

          %ul.dropdown-menu.pull-right
            %li= link_to content_tag(:span, "", class: "glyphicon glyphicon-wrench") + " Site Profile", "#"

            %li.divider

            %li= link_to content_tag(:span, "", class: "glyphicon glyphicon-earphone") + " Invite Users", "#"

    %li.dropdown
      = link_to "Edit User profile", "#", class: "data-toggle", data: { toggle: "dropdown" }

      %ul.dropdown-menu.pull-right
        - unless false

          %li= link_to content_tag(:span, "", class: "glyphicon glyphicon-edit") + " Profile", "#"

          %li.divider

        %li= link_to content_tag(:span, "", class: "glyphicon glyphicon-log-out") + " Sign Out", destroy_user_session_path, method: :delete
